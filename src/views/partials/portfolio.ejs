    <!-- Portfolio Grid -->
<section class="bg-light page-section" id="portfolio">
  <div class="container">
    <div class="row">
      <div class="col-lg-12 text-center">
        <h2 class="section-heading text-uppercase">Tour Sites</h2>
        <h3 class="section-subheading text-muted">Historical tour sites across Nigeria.</h3>
      </div>
    </div>
    <div class="row">
      <%for(let i=0; i<title.length; i++) {%>
        <a href="/tour/<%=title[i]._id%>">
      <div class="col-md-4 col-sm-6 portfolio-item">
        <a class="portfolio-link" data-toggle="modal" href="#port<%=i%>">
          <div class="portfolio-hover">
            <div class="portfolio-hover-content">
              <i class="fas fa-plus fa-3x"></i>
            </div>
          </div>
          <img class="img-fluid" src="<%=title[i].imageUrls[0]%>" alt="">
        </a>
        <div class="portfolio-caption">
          <h4 class="name"><%=title[i].name%></h4>
          <p class="text-muted desc"><%=title[i].description%></p>
        </div>
      </div>
    </a>
      <%}%>
    </div>
  </div>
</section>